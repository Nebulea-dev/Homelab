services:
  hugodocs:
    container_name: hugodocs
    build: .
    command: server --buildDrafts --bind 0.0.0.0
    user: "1000:100"
    volumes:
      - /project/themes
      - ./hugodocs:/project
    ports:
      - 1313:1313
    restart: unless-stopped
    networks:
      - homelab-net
